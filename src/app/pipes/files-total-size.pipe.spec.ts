import { FilesTotalSizePipe } from './files-total-size.pipe';

describe('FilesTotalSizePipe', () => {
  it('create an instance', () => {
    const pipe = new FilesTotalSizePipe();
    expect(pipe).toBeTruthy();
  });
});
