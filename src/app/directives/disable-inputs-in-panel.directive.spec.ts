import { DisableInputsInPanelDirective } from './disable-inputs-in-panel.directive';

describe('DisableInputsInPanelDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableInputsInPanelDirective();
    expect(directive).toBeTruthy();
  });
});
