import { ColorByPackageNoDirective } from './color-by-package-no.directive';

describe('ColorByPackageNoDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorByPackageNoDirective();
    expect(directive).toBeTruthy();
  });
});
